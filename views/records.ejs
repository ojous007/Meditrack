<%-include('partials/homeHeader') %>

<div class="topnav">
  <div class="logo">
    <img src="/css/home/images/meditrackLogo.png" alt="Logo" />
    <h2>MediTrack</h2>
  </div>
  <div class="nav-links">
    <a href="#home">Home</a>
    <a href="#news">Records</a>
    <a href="#contact">Blogs</a>
    <a href="#about">About</a>
  </div>
</div>

<form class="documentForm" method="post" action="/upload" enctype="multipart/form-data">
  <input type="text" placeholder="Name" name="name" id="name"/>
  <input type="text" placeholder="File Name" name="fileName" id="fileName"/>
  <input type="file" placeholder="Upload document" id="document" name="document" />
  <button type="submit">Submit</button>
</form>
<!-- <form method="post" id="documentForm" action="/upload" enctype="multipart/form-data">

  <input type="file" name="file" />
  <input type="submit" value="Upload File" />
</form> -->

<!-- <script>
      const documentForm = document.getElementById('documentForm');

      documentForm.addEventListener('submit', async (e) => {
          e.preventDefault();

          const formData = new FormData(documentForm);

          try {
              const response = await fetch('/upload', {
                  method: 'POST',
                  body: formData,
              });

              if (response.ok) {
                  const data = await response.json();
                 
              }  else {
                  const errorData = await response.json();
                 
              }
          } catch (error) {
              console.error(error);
          }
      });
      
</script> -->
